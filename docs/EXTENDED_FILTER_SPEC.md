# Спецификация: Расширенный фильтр каталога городов — ReloCalc

*Зафиксировано: июль 2026 (v3 — исключены happiness/education до city-level данных, зафиксированы пороги)*

Этот документ фиксирует продуктовый дизайн расширенного фильтра — что должно быть в нём по смыслу и как оно выглядит, до того как соответствующие данные появятся в БД. Порядок работы: (1) спроектировать полный список категорий и UI — этот документ, (2) доработать БД под спроектированное, (3) реализовать фильтр одним спринтом.

Референс по духу — фильтр каталога на relokatz.com/catalog (структура блоков, часть формулировок).

---

## Общие UI-паттерны (действуют для всего фильтра)

- **Категориальные поля с несколькими значениями** (язык, климат, свобода, полит. режим, тур. виза, основания ВНЖ и т.п.) — выпадающий список с мультиселектом внутри (чекбоксы в открытой панели). Дефолт всегда "Любой/ая/ое" (фильтр не применяется, пока не выбрано хоть одно значение).
- **Индексы/уровни качества** (безопасность, экология, медицина, сила паспорта) — выпадающий список с готовыми уровнями-порогами: "Любой / Допустимый (от X) / Высокий (от Y)". Пороги откалиброваны — см. раздел «Калибровка порогов» ниже.
- **Бесплатность** (медицина, сад, школа) — отдельные чекбоксы, независимые от индексов качества. Оба типа фильтра (бесплатно vs качество) сосуществуют, не взаимоисключают друг друга.
- **Числовые диапазоны без естественных категорий** (температура лета/зимы) — два счётчика (от/до) с шагом 1°, поля стартуют **пустыми** (не предзаполненными), с зафиксированными разумными границами ввода (например, −40…+50°C — не дают ввести абсурдное значение).
- **Бюджет** — степпер (−/значение/+), визуально идентичный полю бюджета в быстром фильтре, не слайдер.
- **Гражданство (сроки)** — пилюли с накопительным порогом: Любое / До 2 лет / До 5 лет / До 10 лет / От 10 лет. "До 5 лет" включает все страны с меньшим сроком тоже (не узкий диапазон 3–5).
- **Выбранные значения "улетают" пилюлями наверх фильтра**, под заголовок "Фильтры", в отдельную зону "Выбрано" — с крестиком на каждой пилюле для быстрого снятия конкретного фильтра.

---

## Блок 1. Бюджет

Дублирует поле "Ежемесячный бюджет" из быстрого фильтра (степпер) — для удобства при работе через расширенный фильтр отдельно от быстрого (актуально на мобилке).

---

## Блок 2. Страны и языки

- **Выбор стран** — выпадающий список, мультиселект, двухуровневая структура: сначала кластеры, затем отдельные страны под каждым.
  - **Готово:** `country_clusters` (справочник) + `countries.cluster_id` — созданы и заполнены. Кластеры пользовательские, географическо-исторические (не строго географические и не политико-экономические): Латинская Америка, Евросоюз, Бывший СНГ, Азия (последний пока без стран — задел на будущее).
- **Официальные языки** — выпадающий список, мультиселект, по `country_languages` → `languages`.
- **Уровень английского** — выпадающий список (Любой/Высокий/Средний/Низкий) по `cities.english_level` — готово.

---

## Блок 3. Климат и природа

- **Тип климата** — выпадающий список, мультиселект, по `climate_type_id` → `climate_types` (4 категории; заполнено 11/16 городов).
- **Природа** — выпадающий список, мультиселект (Море/Горы) — по `sea_type`, `has_mountains` — готово.
- **Экология** — выпадающий список с уровнями (Любая/Допустимая/Высокая).
  - **Готово:** `cities.pollution_index` (0–100, переименовано из `ecology_index`) — заполнено по всем 16 городам, источник Numbeo Pollution Index (city-level). **Индекс инвертирован** — выше значение = хуже экология, логика сравнения при фильтрации обратная (см. раздел «Калибровка порогов»).
- **Температура летом** — два счётчика "от"/"до", пустые по умолчанию, шаг 1°, границы ввода (−40…+50) — по `temp_summer_min/max`, поля есть, UI отсутствует.
- **Температура зимой** — аналогично, по `temp_winter_min/max`.

---

## Блок 4. Политика и безопасность

- **Свобода** — выпадающий список, мультиселект, по `country_politics.fh_status_id` → `fh_statuses` (Свободная/Частично свободная/Несвободная) — готово.
- **Политический режим** — выпадающий список, мультиселект, по `eiu_regime_type_id` → `eiu_regime_types` — готово (уже отображается тегом в `CityCard`, не используется как фильтр).
- **Безопасность** — выпадающий список с уровнями (Любая/Допустимая (от X)/Высокая (от Y)) по `safety_index` — готово, заполнено по всем 16 городам, источник Numbeo Safety Index (city-level). Пороги зафиксированы.

*Индекс счастья убран из этого блока — см. раздел «Отложено до city-level данных».*

---

## Блок 5. Легализация и гражданство

Порядок внутри блока: туристические визы → основания для ВНЖ → фильтр "доход подходит" → сила паспорта → гражданство (сроки) → налоги.

- **Туристическая виза** — выпадающий список, мультиселект (Любая/Безвизовый въезд/Условный безвиз/Шенген и т.п. — по факту заполненных `visa_type` в `tourist_visas`) — готово.
- **Основание для ВНЖ** — выпадающий список, мультиселект, по `residency_types` (точный список — открытый вопрос, см. ниже).
  - **"Особые программы"** (стартапер/пенсионер/эксперт/молодёжь/рантье) — НЕ отдельный список данных, а альтернативный вход в тот же справочник оснований, переформулированный "от профиля человека" вместо "от типа основания". Реализуется как UI-надстройка, не требует отдельной таблицы.
  - **Требует новых данных:** `residency_types` заполнен (14 оснований), `country_residency_options` реструктурирован, но реальными условиями по странам пока не наполнен — отдельная исследовательская задача, не структурная.
- **Чекбокс "Доход семьи подходит"** — независимый активный фильтр (не привязан к тому, выбрано ли конкретное основание вручную): показывает только те основания/страны, где доход/бюджет семьи проходит порог `min_income_usd` — **по всем** основаниям сразу, а не только по выбранному. Логика похожа на `getBudgetStatus`, но сравнение идёт с порогом визовой программы, а не со стоимостью жизни.
- **Сила паспорта** — выпадающий список с уровнями (Любая/Средняя (от X)/Сильная (от Y)) по `passport_strength` — готово, нужны пороги (не входили в текущую калибровку — см. открытые вопросы).
- **Гражданство (сроки)** — пилюли, накопительный порог: Любое / До 2 лет / До 5 лет / До 10 лет / От 10 лет — по `citizenship_years` — готово.
- **Налоги** — чекбокс/выпадающий список по `remote_income_tax_type` (значения `none`/`reduced`/`full`) — данные ждут ручного заполнения, UI отсутствует.

---

## Блок 6. Медицина

- **Уровень медицины** — выпадающий список с уровнями (Любой/Допустимый/Высокий).
  - **Готово:** `cities.healthcare_quality_index` (0–100) — заполнено по всем 16 городам, источник Numbeo Health Care Index (city-level). Бесплатность/доступность остаётся отдельным полем `countries.healthcare_access` на уровне страны (законодательный статус) — не смешивается с качеством. Пороги зафиксированы.
- **Бесплатная медицина** — отдельный чекбокс, по `healthcare_access` — готово (тот же источник, что и в расчёте бюджета).
- **Бесплатный сад** — чекбокс, по `kindergarten_is_free` — готово.
- **Бесплатная школа** — чекбокс, по `school_is_free` — готово.

*Уровень образования убран из этого блока — см. раздел «Отложено до city-level данных». Блок переименован из «Медицина и образование» в «Медицина».*

---

## Блок 7 — упразднён

"Другое" убран как отдельный блок: членство в ЕС переехало в Блок 2 (как один из кластеров стран), сила паспорта — в Блок 5 (рядом с остальной легализацией).

---

## Отложено до появления city-level данных

Два индекса временно исключены из активных блоков фильтра и из панели сортировки. Причина — данные в БД по факту являются национальными показателями (World Happiness Report и UNDP HDI считаются только на уровне стран), продублированными по всем городам одной страны. Это противоречит замыслу этих колонок (различие между городами внутри одной страны) и может ввести пользователя в заблуждение.

- `cities.happiness_index` (0–10, шкала Кантрила) — данные не удаляются из БД, колонка остаётся, но не подключается к UI фильтра и сортировки до появления реального city-level источника.
- `cities.education_quality_index` (0–100) — аналогично, не подключается до появления city-level источника (сейчас данные — нормированный UNDP HDI Education Index на уровне страны).

**Рекомендация:** добавить SQL-комментарий на обе колонки в БД (`COMMENT ON COLUMN ...`) с пометкой "данные national-level, не city-level — не использовать в проде до замены источника", чтобы не забыть причину при возврате к этой задаче.

---

## Панель над каталогом (не часть сайдбара, отдельный элемент)

Строка над сеткой карточек: **"Подходящих городов: N"** слева, **"Сортировка по: ..."** справа (выпадающее меню).

Варианты сортировки: Популярность (дефолт), Арендная плата, Безопасность, Свобода (по `fh_score`, не по категориальному режиму), Качество жизни, Медицина, Экология, Стоимость жизни, Бюджет в месяц.

*"Индекс счастья" и "Образование" убраны из списка сортировки — см. раздел «Отложено до появления city-level данных».*

**Требует новых данных:** "Качество жизни" — отдельный сводный индекс, которого нет в схеме — нужно решить, это агрегат из других индексов (безопасность+медицина+экология и т.п.) или отдельная независимая метрика.

---

## Калибровка порогов (зафиксировано, июль 2026)

Методология: 33-й и 66-й процентили. Изначально предполагалось калибровать по мировому распределению (WHR/UNDP/Numbeo глобально), но по факту зафиксированы пороги по абсолютной шкале источника с проверкой на реальных 16 городах проекта. Индекс счастья и образование исключены из калибровки — см. выше.


| Метрика                          | Шкала | Источник                 | Допустимый | Высокий | Логика                                    |
| -------------------------------- | ----- | ------------------------ | ---------- | ------- | ----------------------------------------- |
| **Безопасность**                 | 0–100 | Numbeo Safety Index      | 45.0–65.0  | > 65.0  | Прямая: выше = безопаснее                 |
| **Экология** (`pollution_index`) | 0–100 | Numbeo Pollution Index   | 40.0–65.0  | < 40.0  | **Инвертированная**: ниже значение = чище |
| **Медицина**                     | 0–100 | Numbeo Health Care Index | 55.0–72.0  | > 72.0  | Прямая: выше = качественнее               |


Реальный разброс по 16 городам проекта (для справки, не как источник порогов):

- Безопасность: 38–68, среднее 54.9
- Экология (pollution_index): 16.2–67.8, среднее 36.0
- Медицина: 45.0–78.4, среднее 61.3

**Сила паспорта** (`passport_strength`) — калибровка не проводилась в этом заходе, остаётся открытым вопросом.

---

## Сводка: что требует доработки БД перед реализацией фильтра

1. ~~Поле региона/кластера у `countries`~~ — **готово** (`country_clusters` + `cluster_id`, июль 2026)
2. ~~Наполнение `residency_types`~~ — **готово**, 14 оснований заведено (июль 2026). Осталось наполнить `country_residency_options` реальными условиями по странам — отдельная исследовательская задача, не структурная.
3. ~~Уточнение точного списка оснований для ВНЖ~~ — **готово**, список зафиксирован (см. residency_types_migration.sql)
4. ~~Индекс экологии~~ — **готово**, переименован в `pollution_index`, заполнен по всем 16 городам, пороги зафиксированы.
5. **Индекс счастья** — данные есть, но national-level, не city-level — отложен до появления настоящего источника (см. раздел выше).
6. ~~Индекс качества медицины~~ — **готово**, заполнен по всем 16 городам, пороги зафиксированы. **Индекс качества образования** — данные есть, но national-level — отложен аналогично п.5.
7. ~~Индекс "качество жизни" для сортировки~~ — **решено:** НЕ отдельное поле в БД, считается на фронте как агрегат из уже существующих индексов, конкретная формула/веса определяются на этапе реализации фильтра
8. Дозаполнение `climate_type_id` для оставшихся 5 из 16 городов (Блок 3)
9. ~~UI и данные по налогам на удалённый доход~~ — **готово**, поле переопределено на явные значения `none`/`reduced`/`full`, все страны сброшены в NULL, ждут ручного заполнения

**Единственный оставшийся структурный пункт — дозаполнение** `climate_type_id` **(пункт 8) — не требует новых таблиц/колонок, просто ручной ввод данных.**

## Открытые вопросы (решить перед или во время наполнения БД)

- Точный финальный список "оснований для ВНЖ" — какие категории берём, какие отсекаем (спорные: рождение ребёнка, ЛГБТК+, политическое убежище)
- Пороги для силы паспорта (`passport_strength`) — не откалиброваны
- Где взять честные city-level источники для happiness и education (или отказаться от идеи различий внутри страны и явно завести эти индексы как national-level с соответствующей пометкой в UI)
- "Качество жизни" для сортировки — агрегат существующих индексов или отдельная метрика

